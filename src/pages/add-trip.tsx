import {Save} from "lucide-react"
import {Button} from "@/components/ui/button"
import {Card,CardContent} from "@/components/ui/card"
import {PageHeader,PageShell} from "@/components/layout/page-shell"
export function AddTripPage(){return <TripForm title="Add Trip" eyebrow="Create journey" action="Create trip"/>}
export function TripForm({title,eyebrow,action}:{title:string;eyebrow:string;action:string}){return <PageShell><PageHeader title={title} eyebrow={eyebrow}/><Card><CardContent className="grid gap-4 pt-5 md:grid-cols-2"><Field label="Trip title" placeholder="Monsoon Trails"/><Field label="Destination" placeholder="Kerala, India"/><Field label="Start date" type="date"/><Field label="End date" type="date"/><Field label="Trip status" placeholder="draft, planned, active"/><Field label="Primary city" placeholder="Search city"/><label className="md:col-span-2"><span className="text-sm font-semibold">Visibility notes</span><textarea className="mt-2 min-h-28 w-full rounded-lg border border-border bg-background px-3 py-2 outline-none focus:ring-2 focus:ring-gold" placeholder="Public itinerary toggles stay owner approved"/></label><div className="md:col-span-2"><Button><Save className="h-4 w-4"/>{action}</Button></div></CardContent></Card></PageShell>}
function Field({label,placeholder,type="text"}:{label:string;placeholder?:string;type?:string}){return <label><span className="text-sm font-semibold">{label}</span><input type={type} placeholder={placeholder} className="mt-2 h-11 w-full rounded-lg border border-border bg-background px-3 outline-none focus:ring-2 focus:ring-gold"/></label>}
