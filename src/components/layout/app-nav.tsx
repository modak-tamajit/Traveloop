import {BarChart3,Compass,Home,LogOut,Menu,Moon,Plus,Settings,Sun,User} from "lucide-react"
import {NavLink} from "react-router-dom"
import {Button} from "@/components/ui/button"
import {useAuth} from "@/providers/auth-provider"
import {useTheme} from "@/providers/theme-provider"
import {cn} from "@/lib/utils"
const navItems=[{label:"Dashboard",href:"/dashboard",icon:Home},{label:"Add Trip",href:"/add-trip",icon:Plus},{label:"Analytics",href:"/admin/analytics",icon:BarChart3,adminOnly:true},{label:"Settings",href:"/settings",icon:Settings},{label:"Profile",href:"/profile",icon:User}]
export function AppNav(){const {profile,isAdmin,signOut}=useAuth();const {theme,toggleTheme}=useTheme();const visibleItems=navItems.filter(item=>!item.adminOnly||isAdmin);return <><header className="sticky top-0 z-40 border-b border-border bg-background/90 backdrop-blur"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-4 sm:px-6 lg:px-8"><NavLink to="/dashboard" className="flex items-center gap-3 font-bold"><span className="grid h-10 w-10 place-items-center rounded-xl bg-primary text-primary-foreground"><Compass className="h-5 w-5"/></span><span>Safarnama</span></NavLink><nav className="hidden items-center gap-1 lg:flex">{visibleItems.slice(0,4).map(item=><NavLink key={item.href} to={item.href} className={({isActive})=>cn("rounded-lg px-3 py-2 text-sm font-semibold text-foreground/70 hover:bg-muted hover:text-foreground",isActive&&"bg-muted text-foreground")}>{item.label}</NavLink>)}</nav><div className="flex items-center gap-2"><Button variant="ghost" size="icon" onClick={toggleTheme} aria-label="Toggle theme">{theme==="dark"?<Sun className="h-5 w-5"/>:<Moon className="h-5 w-5"/>}</Button><Button variant="ghost" size="icon" className="lg:hidden" aria-label="Open menu"><Menu className="h-5 w-5"/></Button><Button variant="outline" className="hidden sm:inline-flex" onClick={signOut}><LogOut className="h-4 w-4"/>{profile?.fullName.split(" ")[0]}</Button></div></div></header><nav className="fixed inset-x-0 bottom-0 z-50 border-t border-border bg-background/95 px-2 py-2 backdrop-blur lg:hidden"><div className="mx-auto grid max-w-md grid-cols-5 gap-1">{visibleItems.slice(0,5).map(item=>{const Icon=item.icon;return <NavLink key={item.href} to={item.href} className={({isActive})=>cn("flex h-14 flex-col items-center justify-center rounded-lg text-[11px] font-semibold text-foreground/60",isActive&&"bg-muted text-primary")}><Icon className="h-5 w-5"/><span>{item.label}</span></NavLink>})}</div></nav></>}
