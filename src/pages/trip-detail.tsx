import {BookOpen,CheckSquare,Coins,Route,Share2} from "lucide-react"
import {Link,useParams} from "react-router-dom"
import {Card,CardContent} from "@/components/ui/card"
import {PageHeader,PageShell} from "@/components/layout/page-shell"
const tabs=[["Overview",""],["Itinerary","itinerary"],["Expenses","expenses"],["Packing","checklist"],["Journal","journal"],["Share","share"]]
export function TripDetailPage(){const {id}=useParams();return <PageShell><PageHeader title="Trip Overview" eyebrow="Journey shell"/><div className="mb-5 flex gap-2 overflow-x-auto">{tabs.map(([label,path])=><Link key={label} to={path?`/trip/${id}/${path}`:`/trip/${id}`} className="whitespace-nowrap rounded-lg border border-border bg-card px-4 py-2 text-sm font-semibold">{label}</Link>)}</div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{[{label:"Itinerary",icon:Route,value:"Day-wise builder ready"},{label:"Expenses",icon:Coins,value:"Private by default"},{label:"Packing",icon:CheckSquare,value:"Checklist shell"},{label:"Journal",icon:BookOpen,value:"Notes and moods"}].map(item=>{const Icon=item.icon;return <Card key={item.label}><CardContent className="pt-5"><Icon className="h-6 w-6 text-accent"/><h2 className="mt-3 font-bold">{item.label}</h2><p className="mt-1 text-sm text-foreground/60">{item.value}</p></CardContent></Card>})}</div><Card className="mt-5"><CardContent className="flex flex-col gap-3 pt-5 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="font-bold">Public itinerary controls</h2><p className="text-sm text-foreground/60">Only owner-approved itinerary fields are intended for shared pages.</p></div><Share2 className="h-6 w-6 text-gold"/></CardContent></Card></PageShell>}
